import { StyleSheet } from "react-native";

export const stylesContainer = StyleSheet.create({
    container: {
        alignItems: 'center'
    },
    text: {
        color: '#FFF',
        fontSize: 80
    },
    imgContainer: {
        height: 180,
        width: 180,
    },
    titleText: {
        fontSize: 10,
        fontWeight: "bold"
    }
});