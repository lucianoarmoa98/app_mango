import { StyleSheet } from "react-native";

export const stylesContainer = StyleSheet.create({
    container: {
        alignItems: 'center',
        //flex: 1
    },
    textBusqueda: {
        fontSize: 10,
        fontWeight: "bold",
        marginLeft: 10
    },
    imgContainer: {
        height: 180,
        width: 180,
    },
    titleText: {
        fontSize: 10,
        fontWeight: "bold"
    }
});